import { Island, IslandMeta } from "@/data/model";

export default function Article({ island, content }: { island: IslandMeta, content: Island }) {
    return (
        <div className=""></div>
    )
}
